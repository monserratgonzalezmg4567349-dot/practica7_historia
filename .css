
        body {
            font-family: 'Verdana', sans-serif;
            background-color: #e3f2fd;
            color: #1a3a5a;
            padding: 20px;
            text-align: center;
        }

        h1 { color: #0277bd; text-shadow: 1px 1px 2px #fff; }
        
        p {
            background: rgba(255, 255, 255, 0.7);
            padding: 20px;
            border-left: 5px solid #81d4fa;
            max-width: 800px;
            margin: 20px auto;
            border-radius: 4px;
        }

        img {
            border: 5px solid #b3e5fc;
            border-radius: 50px 5px;
            margin: 15px;
            box-shadow: 0 8px 15px rgba(0,0,0,0.1);
        }

        table {
            margin: 20px auto;
            width: 60%;
            border-collapse: collapse;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        th { background: #0288d1; color: white; padding: 15px; }
        td { background: white; padding: 10px; border-bottom: 1px solid #ddd; }

        ol {
            display: inline-block;
            text-align: left;
            background: #ffffff;
            padding: 20px 50px;
            border-radius: 15px;
            border: 2px dashed #0288d1;
        }

        nav a {
            text-decoration: none;
            color: #0277bd;
            font-weight: bold;
            border: 2px solid #0277bd;
            padding: 8px 15px;
            border-radius: 5px;
            transition: 0.3s;
        }
        nav a:hover {
            background: #0277bd;
            color: white;
        }


    